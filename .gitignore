node_modules/
dist/
.vite/
.DS_Store
*.log

# Local secrets — never commit
.groq
.groq*
.web3forms
.env.local
.env*.local
ops/proxy-1/groq-key.conf

# Vite build output must never be committed at repo root
/assets/

